# Normalize text to LF in the repo (autodetect text files)
* text=auto

# Force LF for source code
*.py  text eol=lf
*.md  text eol=lf
*.txt text eol=lf
*.sh  text eol=lf
*.ps1 text eol=lf

# Jupyter notebooks — choose ONE option:
# Option A: keep notebooks normalized, checkout as LF everywhere
*.ipynb text eol=lf
# Option B: no EOL normalization (leave bytes as saved by the tool)
# *.ipynb -text

# Keep CRLF where Windows expects it
*.bat text eol=crlf
*.cmd text eol=crlf
